# Sprint Plan: Soki S1 — Playable Demo
**Dates:** 2026-04-29 (Wed) → 2026-05-05 (Tue) | **Team:** 1 (solo, S)
**Sprint Goal:** End-to-end Soki round playable by 4 humans on one Realm (Haraya): join → start round → ask/answer → vote → round end → score line.

---

## Capacity
| Person | Available | Allocation | Notes |
|---|---|---|---|
| S | 7 days | ~7.5h (5–10h/wk midpoint) | Solo dev, also job-hunting; buffer 30% |
| **Total** | **7 days** | **~5h committed / 7.5h budget** | Plan to 70% |

---

## Sprint Backlog
| Pri | Item | Est | Notes / Deps |
|---|---|---|---|
| P0 | `POST /api/games/:code/questions` — player→player Q (insert into `questions`) | 1.0h | DB ready; mirror existing route style |
| P0 | `POST /api/games/:code/vote` — submit vote_question option | 1.0h | Validate active vote; idempotent per player |
| P0 | `POST /api/games/:code/round/end` — tally votes, eval `soki_trigger_rule`, write `sequence_evaluation`, advance | 2.0h | Most complex; binary trigger per [MECHANICS] |
| P0 | Minimal `/play` UI — round view, ask Q, answer Q, vote, end-round button (host) | 2.0h | Reuse `/join` style.css; polling 2.5s same as lobby |
| P0 | **FLAG:** lower `MIN_PLAYERS` 10→4 for demo (override [DECISIONS][LOBBY]) | 0.1h | Conflict: PHILOSOPHY MVP=2–4 vs DECISIONS=10. Goal needs 4. Log in MEMORY before merging. |
| P1 | Smoke test w/ 4 browsers (or 1 host + 3 incognito) | 0.5h | Capture bugs as P2 carryover |
| P2 | Stretch: Supabase Realtime on `game_state` (replace polling) | 1.5h | Drop if P0 slips |
| P2 | Stretch: Q1 pre-game oracle vote wiring | 1.0h | Drop if P0 slips |

**Committed:** ~6.6h P0 + 0.5h P1 = 7.1h | **Budget:** 7.5h | **Load:** ~95% — tight; cut P1 first if blocked.

---

## Risks
| Risk | Impact | Mitigation |
|---|---|---|
| `soki_trigger_rule` eval logic ambiguous in code (only seeded, never read) | Round-end blocks demo | Read SCHEMA.md `[GAME_LOOP]` Mon AM; if unclear, hardcode binary threshold for sprint, flag in DECISIONS |
| MIN_PLAYERS=10 conflicts w/ 4-player demo | Demo cannot run | Flag override in MEMORY [DECISIONS] before edit |
| Job apps (Amby etc.) eat the 7.5h | Sprint slips | Hard-cap stretch; protect 2h Sat for round/end |
| 04 Haraya seed is DRAFT | Sequence eval may need data not approved | Demo on existing seed only; no new SQL writes |
| Solo dev = no review | Bugs ship | Manual smoke test required before "done" |

---

## Definition of Done
- [ ] 4-player game runs end-to-end in 2 browsers without server crash
- [ ] All 3 P0 endpoints return 200 on happy path + 4xx on guard cases
- [ ] `/play` UI updates within 3s of state change
- [ ] MEMORY.md [LOG] entry per session
- [ ] No new tables; existing 21 entities only

---

## Key Dates
| Date | Event |
|---|---|
| Wed Apr 29 | Sprint start — `POST /questions` + `POST /vote` |
| Fri May 1 | Mid-sprint check — `/round/end` should be drafted |
| Sun May 3 | UI + smoke test |
| Tue May 5 | Sprint end / demo / retro + MEMORY log |

---

## Flags Requiring Approval
1. **MIN_PLAYERS override**: 10 → 4 for demo. DECISIONS.md [LOBBY] entry must be edited *with note*, not silently. Proposed: keep 10 in prod, add `DEMO_MODE=true` env flag → 4.
2. **soki_trigger threshold**: if not yet specified, sprint picks default (e.g. ≥75% agreement = Soki) and logs in DECISIONS [MECHANICS] for confirmation.
